# Reasonix configuration.
# Resolution order: flag > ./reasonix.toml > ~/.reasonix/config.toml > built-in defaults.
# Secrets come from the environment via api_key_env; never put keys here.

config_version = 3   # schema marker for diagnostics; old versions may ignore it
default_model = "deepseek-flash"
# language      = "zh"   # ui/model language; empty = auto-detect from $LANG / $REASONIX_LANG

[agent]
# system_prompt = """..."""   # omit to use the built-in prompt for this version
# system_prompt_file = "prompts/system.md"   # overrides system_prompt when set
# max_steps         = 0   # executor tool-call rounds; 0 = no limit
# planner_max_steps = 12   # planner read-only tool-call rounds; 0 = no limit
temperature       = 0.0
auto_plan   = "off"   # off|on; off keeps plan mode manual
# reasoning_language = "zh"   # visible reasoning language: auto|zh|en
# auto_plan_classifier = "deepseek-flash"   # optional; only used for borderline tasks
soft_compact_ratio  = 0.5   # notice only; keeps cache-first prefix intact
compact_ratio       = 0.8   # try compacting when prompt reaches this fraction
compact_force_ratio = 0.9   # force compacting at this high-water mark
# keep                = ["errors"]   # compaction keep policy: errors, user_marked
# recent_keep         = 2   # minimum recent messages kept verbatim
cold_resume_prune   = true   # elide stale tool results when reopening a session past the provider cache window
# planner_model = "mimo"   # optional: enable two-model collaboration
# subagent_model = "deepseek-pro"   # optional default for runAs=subagent skills
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }   # per-skill overrides
# subagent_effort = "high"   # optional default effort for subagents
# subagent_efforts = { review = "max", task = "high" }   # per-tool/skill effort overrides
# output_style = "explanatory"   # explanatory | learning | concise | custom; empty = default

[tools]
enabled = []   # empty = all built-in tools
bash_timeout_seconds = 120   # foreground safety cap; set 0 for no tool-local cap

[tools.background_jobs]
stalled_warning_seconds = 900   # warn once per background job after this many quiet seconds; 0 disables

[lsp]
enabled = true   # language server tools; servers launch lazily when used
# [lsp.servers.go]
# command = "gopls"
# args = []
# extensions = [".go"]

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # extra custom skill roots
# excluded_paths = ["~/.agents/skills"]   # hide convention roots without deleting folders
# max_depth = 3   # nested scan depth; set 1 for legacy root-only discovery
# disabled_skills = ["review"]   # hide noisy or unwanted skills

[permissions]
# Per-call gating. mode = writer fallback when no rule matches: ask|allow|deny.
# Readers always default to allow. Precedence: deny > ask > allow > fallback.
# Rules are "Tool" or "Tool(specifier)"; e.g. Bash(go test:*), Edit(src/**).
mode  = "allow"
# deny = ["Bash(rm -rf*)", "Bash(git push*)"]   # hard-blocked in every mode
allow = ["task", "Bash(rm -f:*)", "Bash(cd /Users/thomas/Projects/claude/projects/yijing-bugua-new && npx tsc --noEmit 2>&1 | head -50)", "Bash(cd /Users/thomas/Projects/claude/projects/yijing-bugua-new && git status)", "Bash(cd /Users/thomas/Projects/claude/projects/yijing-bugua-new && git remote -v)", "Bash(cd /Users/thomas/Projects/claude/projects/yijing-bugua-new && git add -A && git status)", "Bash(cd /Users/thomas/Projects/claude/projects/yijing-bugua-new && git commit -m \"refactor: 改用 Supabase 云端数据库直连，移除本地同步\n\n- 重写 records.ts 为 Supabase 直连，所有操作需要 userId\n- 删除本地 IndexedDB schema 和同步逻辑\n- 更新所有调用处传入 userId 参数\n- 移除设置页面的同步 UI\n- 支持多端使用同一云端数据库，通过 user_id 区分用户\")", "Bash(cd /Users/thomas/Projects/claude/projects/yijing-bugua-new && git push origin main)", "Bash(cd /Users/thomas/Projects/claude/projects/yijing-bugua-new && npm run build 2>&1 | tail -30)", "Bash(cd /Users/thomas/Projects/claude/projects/yijing-bugua-new && npm run build 2>&1 | tail -20)", "Bash(cd /Users/thomas/Projects/claude/projects/yijing-bugua-new && git add -A && git commit -m \"fix: 修复 schemaVersion 类型错误\" && git push origin main)", "Bash(cd /Users/thomas/Projects/claude/projects/yijing-bugua-new && npm run build 2>&1 | tail -15)", "Bash(cd /Users/thomas/Projects/claude/projects/yijing-bugua-new && git add -A && git commit -m \"feat: API Key 同步到云端\n\n- 保存 API Key 时同时保存到 Supabase user_settings 表\n- 登录时自动从云端加载 API Key\n- 删除 API Key 时同时删除云端数据\n- 多端登录后 API Key 自动同步\" && git push origin main)", "Bash(cd /Users/thomas/Projects/claude/projects/yijing-bugua-new && cat supabase/migrations/*.sql | grep -A 5 \"user_settings\")", "Bash(rm src/components/casting/Coin.tsx:*)", "Edit", "Bash(npx vitest run tests/engine/casting.test.ts 2>&1)", "Bash(npx tsc --noEmit 2>&1)", "Bash(rm src/components/casting/BeforeDivination.tsx:*)", "Bash(npx vitest run tests/db/ 2>&1)", "run_skill", "Bash(npx vitest run tests/engine/ 2>&1)", "Bash(node -e \"\nconst { describe, it, expect, vi, beforeEach } = require('vitest');\n// Can't run this directly — let me write a quick debug test\nconsole.log('Need to debug via vitest');\n\" 2>&1 || true)", "Bash(npx vitest run tests/db/export-import.test.ts 2>&1)", "Bash(npx vitest run tests/db/records.test.ts 2>&1)", "Bash(npx vitest run 2>&1)", "Bash(git remote:*)", "Bash(git add package.json src/ src/ai/fallback-interpretation.ts tests/ && git commit -m \"全面修复: 修正Coin摇卦逻辑、接入ManualInput、改进完成流程、清理死代码、修复406错误、重构DB测试、新增E2E测试、添加内置白话解读\n\nF1  VirtualCoins替代Coin（架构+概率双重错误）\nF2  接入ManualInput（手动输入模式可用）\nF3  6爻完成流程从useEffect迁移到setLineValue\nF4  新增内置白话解读引擎（无API Key时可用）\nF5  重构DB测试（mock Supabase，21/21通过）\nF6  清理4个死代码组件+3个npm死依赖\nF7  修复 user_settings 406错误(.single→.maybeSingle)\nF8  添加密码表单 autocomplete 属性\nF9  删除未使用的 castNextLine 回调\nF10 新增起卦流程E2E测试\nF13 queryPendingDue增加NULL过滤\" 2>&1)", "Bash(git push origin main 2>&1)", "Bash(git add src/components/casting/VirtualCoins.tsx src/index.css && git commit -m \"fix: 3枚铜钱同时翻转动画 — 3D旋转效果替代旧animate-pulse\" && git push origin main 2>&1)", "explore", "Bash(git add src/ && git commit -m \"fix: AI解读不显示 + API Key状态不同步\n\nBug 1: AI解读完成后页面不更新，需刷新才能看到\n  - 根因: double-call.ts 的 onProgress('done') 在 DB 写入前触发\n  - 修复: 将 setProgress('done') 移到 useAIInterpretation 的 await updateRecord 之后\n\nBug 2: 已配置 API Key 但页面显示未配置\n  - 根因: hasKey 读取 localStorage 是同步的，云端异步加载完成后 React 不重渲染\n  - 修复: setApiKey/removeApiKey 派发 CustomEvent，hooks 用 useState+事件监听响应式同步\" && git push origin main 2>&1)", "Bash(npx vitest run 2>&1 && npx tsc --noEmit 2>&1)", "Bash(npx vite build 2>&1)", "Bash(git add src/pages/ResultView.tsx && git commit -m \"fix: AI错误状态时操作按钮区隐藏，导致用户看不到重试按钮\" && git push origin main 2>&1)", "Bash(npx vite build 2>&1 && ls -la dist/assets/index-*.js dist/assets/index-*.css 2>&1)", "Bash(grep -c \"白话解读\" dist/assets/index-*.js 2>&1)", "Bash(grep -c \"暂无 AI 解读\" dist/assets/index-*.js 2>&1)", "Bash(grep -c \"api-key-changed\" dist/assets/index-*.js 2>&1)", "Bash(grep -c \"请先在设置中配置 DeepSeek API Key\" dist/assets/index-*.js 2>&1)", "Bash(grep -o \"暂无 AI 解读\" dist/assets/index-*.js 2>&1)", "Bash(grep -o \"请先在设置页面配置 DeepSeek API Key\" dist/assets/index-*.js 2>&1)", "Bash(grep -o \"暂无 AI 解读\" dist/assets/index-*.js 2>&1; echo \"---\"; grep -o \"api-key-changed\" dist/assets/index-*.js 2>&1; echo \"---\"; grep -o \"白话解读\" dist/assets/index-*.js 2>&1)", "Bash(git log --oneline -6 2>&1)", "Bash(npx vite build 2>&1 && grep -c \"获取 AI 深度解读需要配置\" dist/assets/index-*.js 2>&1)", "Bash(rm src/hooks/useApiKey.ts:*)", "Bash(git add src/ && git commit -m \"fix: 彻底修复 hasKey 状态同步 + AI 解读即时显示\n\n根因:\n1. hasKey 竞态: loadApiKeyFromCloud 异步加载 API Key 到 localStorage,\n   但 useAIInterpretation 的 useState 只在组件挂载时读一次,\n   事件监听器也可能在 setApiKey 调用之后才挂载\n\n2. AI 解读不显示: 解读完成后依赖 useEffect 重新 fetch 记录,\n   但 fetch 可能拿到旧数据(数据库写入与读取之间的竞态)\n\n修复:\n- 将 hasKey 状态提升到 AuthContext, 作为 React context 状态管理\n- AuthContext 监听 api-key-changed 事件 + 云端加载完成后自动更新\n- triggerDefault/triggerDeep 现在直接返回更新后的记录\n  ResultView 立即用 setRecord 更新, 无需重新 fetch\n- 删除已废弃的 useApiKey hook\" && git push origin main 2>&1)", "Bash(npx vite build 2>&1 && ls dist/assets/index-*.js dist/assets/index-*.css 2>&1)", "Bash(curl -s -o /dev/null -w \"%{http_code}\" \"https://yijing-bugua.vercel.app/assets/index-BqyDUakG.js\" 2>&1)", "Bash(curl -sI \"https://yijing-bugua.vercel.app/assets/index-BqyDUakG.js\" 2>&1 | head -5)", "Bash(git log --oneline -5 2>&1)", "Bash(git status 2>&1)", "Bash(cd /Users/thomas/Projects/claude/projects/yijing-bugua-new && git log --oneline -20)"]
# ask = ["Edit(src/**)"]   # force a prompt even if otherwise allowed

[sandbox]
# Confine tool blast radius. File-writers (write_file/edit_file/multi_edit/move_file)
# may only write under workspace_root (empty = current dir) and allow_write extras.
# bash = "enforce" (default) jails each command in an OS sandbox (macOS now;
# graceful fallback elsewhere); "off" disables it. network allows egress.
# workspace_root = ""            # default: current working directory
allow_write = ["/"]
bash    = "off"
network = true

[statusline]
# A custom status line: a command whose first stdout line replaces the built-in
# data row. It receives {"model","contextUsed","contextWindow","cwd"} as JSON on stdin.
# command = "my-statusline.sh"

# External MCP servers. type: "stdio" (default, a subprocess) | "http" | "sse".
# ${VAR} / ${VAR:-default} are expanded from the environment in command/args/env/url/headers.
# [[plugins]]
# name    = "example"
# command = "reasonix-plugin-example"
# [[plugins]]                                  # a remote server over Streamable HTTP
# name    = "stripe"
# type    = "http"
# url     = "https://mcp.stripe.com"
# headers = { Authorization = "Bearer ${STRIPE_KEY}" }
